version: '3.8'

services:
  csv-notes:
    build: .
    container_name: csv-notes-manager
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/notes.db
    restart: unless-stopped
